% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relationships.R
\name{mesh_ancestors_at_depth}
\alias{mesh_ancestors_at_depth}
\title{Find ancestor branches at an absolute tree depth}
\usage{
mesh_ancestors_at_depth(mesh, depth, db = meshdb())
}
\arguments{
\item{mesh}{Character vector of MeSH IDs or exact MeSH term labels.}

\item{depth}{A single positive integer giving the absolute tree depth to
return.}

\item{db}{A \code{meshdb} object returned by \code{\link[=meshdb]{meshdb()}}.}
}
\value{
A tibble with one row per input term, branch parent, and ancestor at
\code{depth}. The \code{distance} column gives the number of direct-parent hops from
the input term to the ancestor.
}
\description{
Given MeSH IDs or exact term labels, climb every parent branch independently
and return ancestors whose independently calculated tree depth equals
\code{depth}. Tree depth is 1-indexed, so root terms have \code{ancestor_depth = 1}.
}
